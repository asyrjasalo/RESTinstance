version: 2

jobs:
  build:
    machine: true

    steps:
      - checkout

      - run:
          name: Run tests
          command: ./scripts/test

      - store_test_results:
          path: results

version: 2

jobs:
  build:
    machine: true

    steps:
      - checkout

      - run:
          name: Run tests with rfdocker
          command: scripts/test --xunit xunit.xml tests/

      - store_test_results:
          path: results/

      - store_artifacts:
          path: results/
          destination: robot-results/
